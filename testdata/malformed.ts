// Missing closing brace
function broken(x: number) {
  return x *

// Incomplete class
class Partial {
  method() {
    // missing closing braces

// Dangling export
export function

// Invalid syntax
const = ;

// Unclosed string
const s = "hello

// Unexpected token
function @@invalid() {}
